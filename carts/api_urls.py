# carts/api/urls.py
from django.urls import path
# from .api_views import CartItemListAPIView, CartItemDetailAPIView,CheckoutAPIView

app_name = "carts_api"

urlpatterns = [
    # path('', CartItemListAPIView.as_view(), name='cart_items'),
    # path('<int:pk>/', CartItemDetailAPIView.as_view(), name='cart_item_detail'),
    # path('checkout/', CheckoutAPIView.as_view(), name='checkout-api'),

]
